import parseCloudRequest from 'web-agro-control-system/utils/parse-cloud-request';
import { module, test } from 'qunit';

module('Unit | Utility | parse cloud request');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parseCloudRequest();
  assert.ok(result);
});
